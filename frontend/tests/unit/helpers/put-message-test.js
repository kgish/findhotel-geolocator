
import { putMessage } from 'frontend/helpers/put-message';
import { module, test } from 'qunit';

module('Unit | Helper | put message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = putMessage([42]);
  assert.ok(result);
});

