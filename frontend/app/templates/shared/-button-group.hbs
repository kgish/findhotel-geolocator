<div class="form-group row">
    <div class="offset-sm-2 col-sm-10">
        <button class='btn btn-success' {{action "save"}}>{{fa-icon 'check'}} {{#if model.isNew}}Create{{else}}Update{{/if}} </button>
        <button class='btn btn-default' {{action "cancel"}}>{{fa-icon 'close'}} Cancel </button>
    </div>
</div>